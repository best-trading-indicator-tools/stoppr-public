// Global colors used across the app. Adjust here to update everywhere.
import 'package:flutter/material.dart';

class AppColors {
  // Semi-transparent white for card / widget backgrounds on dark night sky
  static const Color widgetBg = Color.fromRGBO(255, 255, 255, 0.05);

  // Slightly stronger opacity for borders
  static const Color widgetBorder = Color.fromRGBO(255, 255, 255, 0.1);
}
